/**
 * CbVideoPlayer 视频播放器组件入口
 * 组件实体在当前目录 index.vue（与 audio-player 一致的目录约定），
 * 本入口用于 gen:index 提取组件名生成全局注册/按需导出。
 */
import CbVideoPlayer from './index.vue'

export { CbVideoPlayer }
export default CbVideoPlayer
