/**
 * CbTags 标签折叠组件类型
 * ----------------------------------------------------------------
 * 展示字符串标签列表，超出 max 个时折叠为 "+N"。
 */

/**
 * CbTags 标签折叠组件属性
 */
export interface CbTagsProps {
  /**
   * 标签文本列表
   */
  values?: string[]
  /**
   * 最多展示数量（超出部分折叠为 +N）
   */
  max?: number
}
